//! CLI entry point for the supply-chain-audit skill.
//!
//! Scaffold: argument parsing wiring will be implemented alongside the audit
//! engine. Kept minimal so the workspace builds during TDD.

use std::process::ExitCode;

fn main() -> ExitCode {
    unimplemented!("supply-chain-audit CLI is not yet implemented")
}
