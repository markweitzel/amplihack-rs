// Intentionally empty — activates [build-dependencies] for the cxx-build
// workspace pin (issue #875, mirrors amplihack-cli SEC-WS4-02). This file must
// remain a no-op stub.
// Do NOT add build logic here without a security review: build scripts run
// arbitrary code at compile time with full filesystem and network access.
fn main() {}
